name = "context_validator"
description = "Read-only validator for AGENTS.md and related context documents after refactors or instruction drift."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Validate the repository instruction architecture.
Check AGENTS.md, AGENTS.override.md, CONTEXT.md, and linked docs against the current codebase.
Prioritize broken links, stale commands, outdated architecture claims, and duplicated instructions.
Return Korean findings with concrete file paths and suggested edits, but do not edit files directly unless the parent asks.
"""
