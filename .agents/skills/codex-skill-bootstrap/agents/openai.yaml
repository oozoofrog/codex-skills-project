interface:
  display_name: "Codex Skill Bootstrap"
  short_description: "Create or modernize a Codex skill workspace"
  default_prompt: "Create a new repo-local Codex skill with clean frontmatter, optional openai.yaml metadata, and progressive-disclosure references."
policy:
  allow_implicit_invocation: true
