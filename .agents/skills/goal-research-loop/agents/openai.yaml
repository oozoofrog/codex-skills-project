interface:
  display_name: "Goal Research Loop"
  short_description: "Run a bounded research loop with contract, ledger, handoff, and explicit decision layers"
  default_prompt: "Given a concrete objective, define a measurable evaluation contract, separate hard gates, experiment status, and control action, then run a bounded research loop with a ledger and state snapshot."
policy:
  allow_implicit_invocation: false
