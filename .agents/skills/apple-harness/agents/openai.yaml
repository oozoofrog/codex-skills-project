interface:
  display_name: "Apple Harness"
  short_description: "Plan, design, build, and evaluate long Apple tasks"
  default_prompt: "Run a long Apple platform workflow with PLAN→DESIGN→BUILD→EVALUATE loops, shared artifacts, Xcode verification, and optional Pencil-based design."
policy:
  allow_implicit_invocation: true
