interface:
  display_name: "macOS Release"
  short_description: "Automate macOS release prep with verification gates"
  default_prompt: "Guide or automate macOS release work with verification gates: version bump, build, DMG/ZIP packaging, local install verification, then GitHub release and Homebrew publishing."
policy:
  allow_implicit_invocation: true
