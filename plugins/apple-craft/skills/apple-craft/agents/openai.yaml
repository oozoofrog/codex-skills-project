interface:
  display_name: "Apple Craft"
  short_description: "Swift, SwiftUI, UIKit, AppKit, and Xcode development"
  default_prompt: "Help with Apple platform development using Xcode tools, official docs, and bundled Apple references, and verify changes with build, preview, or diagnostics evidence when possible."
policy:
  allow_implicit_invocation: true
