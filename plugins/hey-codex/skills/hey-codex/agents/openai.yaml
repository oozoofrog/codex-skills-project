interface:
  display_name: "Hey Codex"
  short_description: "Delegate to a separate Codex CLI run when explicitly asked"
  default_prompt: "Invoke a separate Codex CLI process for an isolated second opinion, read-only analysis, or carefully sandboxed full-auto edit only when the user explicitly asks for it."
policy:
  allow_implicit_invocation: true
